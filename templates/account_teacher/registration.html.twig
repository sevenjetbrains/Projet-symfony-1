{% extends "partials_teacher/base.html.twig" %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col-2">
            </div>
			<div class="col-6">


				{{form_start(form)}}

				{{form_widget(form)}}
<div class="d-flex d-flex justify-content-center">

                <button type="submit" class="btn btn-info mr-2">Enregister</button>
<a href="{{path('home_teacher')}}" class="btn btn-info">Annuler</a>

          </div>  

				{{form_end(form)}}
			</div>


		</div>


	</div>


{% endblock %}
